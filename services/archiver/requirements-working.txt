# Matrix client - older stable version
  matrix-nio==0.19.0

  # Supabase - compatible version
  supabase==1.0.3

  # Web framework - compatible with both
  fastapi==0.100.0
  uvicorn==0.23.0

  # Core dependencies
  pyyaml==6.0.1
  python-dotenv==1.0.0
  aiohttp==3.8.5
  asyncpg==0.28.0
  structlog==22.3.0
  typing-extensions==4.7.0

  # Pydantic - compatible version for both FastAPI and Supabase
  pydantic>=1.10.0,<2.0.0
